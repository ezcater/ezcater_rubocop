require "spec_helper"

RSpec.describe EzRubocop do
  it "has a version number" do
    expect(EzRubocop::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
